import { createHashHistory } from "history"; // O createBrowserHistory si usas 
                                             // browser router.
const history = createHashHistory();

export default history;